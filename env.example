# Supabase (public values may be exposed to the browser; service role is secret)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-public-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key

# Email (secret)
RESEND_KEY=re_your-secret-key

# Site
NEXT_PUBLIC_SITE_URL=https://primaryuc.com

# Form security
FORM_BOT_PROTECTION_ENABLED=true
FORM_BOT_PROVIDER=vercel-botid
FORM_BOT_PROTECTION_MODE=enforce
FORM_RATE_LIMIT_ENABLED=true
FORM_DUPLICATE_PROTECTION_ENABLED=true
FORM_SECURITY_HASH_SECRET=replace-with-at-least-32-random-characters
FORM_ALLOWED_ORIGINS=https://primaryuc.com,https://www.primaryuc.com
FORM_ALLOW_VERCEL_PREVIEWS=false
FORM_BOT_PROVIDER_TIMEOUT_MS=1500

# Upstash Redis (secrets; required in production when rate/duplicate protection is enabled)
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-secret-rest-token

# Local testing only. Never configure this in Vercel Production.
# FORM_BOTID_DEV_BYPASS=HUMAN

# Optional: lets one known tester (e.g. verifying GTM/Google Ads conversion
# tracking on the live site) skip BotID + rate limiting + duplicate protection
# for a single request by sending header `x-form-test-token`. Leave unset to
# disable. If set, must be 24+ random characters and kept secret -- never
# commit it, never expose it as a NEXT_PUBLIC_ var.
# FORM_TEST_BYPASS_TOKEN=
